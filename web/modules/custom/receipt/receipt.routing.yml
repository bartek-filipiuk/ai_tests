receipt.process:
  path: "/api/receipts/process"
  defaults:
    _controller: Drupal\receipt\Controller\ReceiptController::processReceipt
    _title: "Process Receipt"
  methods: [POST]
  requirements:
    _permission: "access content"
