<?php
/**
 * @file
 * Hooki dla modułu Simple Config Block.
 */

/**
 * Implementacja hook_theme().
 */
function simple_config_block_theme($existing, $type, $theme, $path) {
  return [
    'random_text_block' => [
      'variables' => ['text' => NULL],
      'template' => 'random-text-block',
    ],
  ];
}
